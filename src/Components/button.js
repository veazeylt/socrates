//button to press and generate a name

import React, { Component } from 'react';

class Button extends Component {
    state = {  }
    render() { 
        return (  
            <div>'Generate Name!'</div>
        );
    }
}
 
export default Button ;
